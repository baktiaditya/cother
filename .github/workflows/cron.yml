name: Sitemap Cron Job

on:
  # Triggers the workflow every 5 minutes
  schedule:
    - cron: '*/5 * * * *'

jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line script
        run: echo Hello, world!
